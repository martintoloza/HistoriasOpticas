= form_for @profesion do |f|
  #error_explanation.bg-danger.text-danger
  .row
    .col-sm-6
      .form-group
        = f.label :codigo, :class => "control-label"
        = f.text_field :codigo, :class => "form-control first_input"
      .form-group
        = f.label :nombre, :class => "control-label"
        = f.text_field :nombre, :class => "form-control"
      .form-group
        = f.submit @profesion.new_record? ? "Crear Profesion" : "Actualizar Profesion", :class => "btn btn-primary"